// @bradygaster/typespec-squad — Squad layer TypeSpec decorators
//
// Public API surface. All decorator types and emitter hooks exported from here.
//
// Decorators (to be implemented):
//   @casting  — bind a TypeSpec AgentDefinition to a squad member identity
//   @routing  — declare work routing rules between agents
//   @ceremony — define squad ceremonies (standup, retro, handoff)
//
// Emitters (to be implemented):
//   squadEmitter  — emits charter.md, team.md, routing.md, registry.json into .squad/

// Placeholder — implementation follows in Phase 2
export {};
