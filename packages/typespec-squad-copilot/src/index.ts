// @bradygaster/typespec-squad-copilot — Copilot SDK TypeSpec emitter
//
// Public API surface. All decorator types and emitter hooks exported from here.
//
// Decorators (to be implemented):
//   @model  — declare the Copilot model assignment for an agent
//             (gpt-4o, gpt-4o-mini, claude-sonnet, etc.)
//
// Emitters (to be implemented):
//   copilotEmitter  — emits squad.config.ts (typed Copilot SDK config)
//                     and copilot-instructions.md (system prompt)
//
// Note: @model is Copilot-specific and lives here, NOT in @bradygaster/typespec-squad.
// This separation keeps the base Squad layer runtime-agnostic.

// Placeholder — implementation follows in Phase 2
export {};
